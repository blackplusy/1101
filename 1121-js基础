1.复合数据类型
	a.常用内置对象
	b.日期对象
	c.数学对象
	d.字符串对象
		封装了和字符串相关的特性，用来处理字符串，对字符串进行剪切、合并、替换等操作
		调用string对象的方法或者属性
		string([val])
		注意:val是可选项
		// string.substring(start,stop)
		// start必须要有，规定需要提取的字符串第一个字符在string中的位置
		// stop可选，非负数，比提取字符串最后一个字符多1，如果省略这个参数，返回字符串一直到结尾
		// slice用法：arrayobject.slice(start,end)
		// 返回一个新的数组，包含start到end不包含该元素的arrayobject中元素
		补充：
		在<p>元素中是不能嵌套块元素的，在<p>标签还没结束时，遇到下一个块级元素就会自动结束
	e.数组对象
		js中基本数据类型，封装了和数组相关的方法和属性
		房间1     房间2    房间3    房间4
		林志玲    周冬雨   苍老师   蔡依林
		创建数组的方式
			1.通过new运算符调用Array对象构造函数
				var a =new Array();
			2.通过构造函数传递元素作为参数(创建带指定元素的数组)
				var a=new Array(10,20,30,40);
			3.直接将元素放入[]中，元素通过逗号分隔开
				var a=[1,2,3,4]
			4.创建指定数组(元素的个数)
				var a=new Array(3)
2.其他数据类型
	a.函数
	函数的角色有2种，一个是数据类型方面，另外一个是程序设计方面
	定义函数的方式
		function typename(arg)
		{
			this.arg=arg;
		}
	b.空值
	之前所说数据类型由自己内容，编程中还有：什么都没有(null)
	注意:一个=代表赋值，两个=代表比较，三个=代表对象比较
	c.不确定数据类型
	undefined  表示一个变量什么都没得到，连空都没有
	在应用里面null和undefined实际意义等效
3.数据类型转换
	1>隐式数据类型转换
	程序运行时候系统根据上下文需要，自动将数据转换为另外一种数据类型的过程
	2>显式数据类型转换
	通常使用parseInt(),parseFloat()
4.常量和变量
	a.常量
		从始至终不能被改变的数据
		a1.常量的种类
		包含字符类型，布尔类型，数值类型null等等
		"天儿不错！"   字符常量
		077           数值常量
		true          布尔常量
	b.变量
		b1.什么是变量
			变量是在程序运行时候发生值的改变的量，专业说法，内存单元
			可以理解为装东西的容器，变量名代表分配给他们的内存单元
		b2.变量的定义
			var 变量名=值;   定义变量
			变量名=值;       赋值
			var 变量名;      定义变量
			字符类型变量
				var str="hello"
			布尔类型变量
				var b=true;
			数值类型变量
				var n=10：
			复合类型变量
				var obj=new object();
		b3.变量的命名规则
			变量名必须以字母或者_或者$开头，后面跟字符或者数字，不能使用特殊符号
			js对标识符大小写敏感
		b4.变量的作用域
			作用域：有效范围，js变量作用域有全局和局部之分，变量名同名时候局部作用域优先于全局作用域
		b5.变量的用途
			存储数据，比如计算街头，存储用户输入等等
预习以下内容：
https://www.w3school.com.cn/js/js_operators.asp
